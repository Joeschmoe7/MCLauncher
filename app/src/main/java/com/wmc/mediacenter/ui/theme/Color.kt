package com.wmc.mediacenter.ui.theme

import androidx.compose.ui.graphics.Color

// WMC (Windows Media Center) inspired palette — tuned against a real WMC
// screenshot: it reads as blue throughout, not teal/cyan-shifted, with a
// soft diffuse highlight rather than a strongly colored glow.
val WmcNavyDark = Color(0xFF04101F)
val WmcNavyMid = Color(0xFF102C4E)
val WmcGlowBlue = Color(0xFF2E5F96)
val WmcTextPrimary = Color(0xFFE8F1F8)
val WmcAccentCyan = Color(0xFF4FC3F7)
val WmcTileSurface = Color(0xFF0C1E33)
